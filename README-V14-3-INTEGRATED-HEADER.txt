FITNESS TIME UAE — V14.3 INTEGRATED DESKTOP HEADER

Desktop fix:
- Theme, colour and language controls are now integrated INSIDE the main header.
- Controls no longer float in a second row under the header.
- Header structure: Logo | Navigation | Theme / Colour / Language | Join Now.
- RTL Arabic keeps the integrated header clean.
- Narrow desktop spacing is reduced to prevent overlap.

Mobile:
- Existing approved mobile header and utility strip are preserved.
- Mobile drawer, theme controls and Arabic switching remain unchanged.

Replace your current project with this clean folder, then run:
npm install
npm run dev

Before GitHub/Vercel deployment also run:
npm run build
