FITNESS TIME UAE — V14.2 FINAL DESKTOP HEADER

Change in this build:
- Desktop header only: protected three-zone layout (logo / navigation / Join Now).
- Desktop theme, colour and language controls remain outside the header so they cannot collide with navigation.
- Narrow laptop spacing scales down cleanly.
- Arabic desktop header keeps RTL alignment.
- Approved mobile layout and mobile breakpoints are unchanged.

Run:
npm install
npm run dev

Production check:
npm run build
